import * as React from 'react';
import  { FC } from 'react';

const Navigation: FC = () => {

    return (
        <nav>
            <h1>Money stream</h1>
        </nav>
    );
};

export default Navigation;
